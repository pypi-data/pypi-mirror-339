[project]
name = "kovacszsolt_add"
version = "0.1.0"
description = "Egy egyszerű csomag, amely összead két számot."
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [
  {name = "Zsolt Kovács", email = "te@peldaemail.hu"}
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
