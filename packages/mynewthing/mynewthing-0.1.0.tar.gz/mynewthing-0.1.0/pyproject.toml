[project]
name = "mynewthing"
version = "0.1.0"
description = ""
authors = [
    {name = "Rodney Martin",email = "rodney.martin@saultcollege.ca"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich (>=14.0.0,<15.0.0)"
]

[tool.poetry]
packages = [{include = "mynewthing", from = "src"}]

[tool.poetry.scripts]
greet = "mynewthing.cli:greet"
another = "mynewthing.cli:somethingelse"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

