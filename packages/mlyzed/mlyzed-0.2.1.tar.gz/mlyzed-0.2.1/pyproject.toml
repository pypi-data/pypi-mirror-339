[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

dependencies = [
  "numpy",
  "scipy",
  "ase",
  "vesin",
  "tqdm",
  "networkx",
]

[project]
version = "0.2"
name = "mlyzed"
authors = [{name = "Artem Dembitskiy", email = "art.dembitskiy@gmail.com"}]
readme = "README.md"
license = {file = "LICENSE"}
keywords = ["MD", "diffusion", "conductivity", "unwrap"]

