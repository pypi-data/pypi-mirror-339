import cohere

# Initialize the Cohere client with your API key
co = cohere.Client('LMrLYbBsVoOLIAPghzjPcKomJr5mXzjWsnmmFPGZ')

def askai(prompt):
    """
    This function sends a prompt to the Cohere API and returns the generated text.

    Args:
    - prompt (str): The input text to be processed by the Cohere model.

    Returns:
    - str: The text generated by Cohere model.
    """
    # Call the Cohere API to generate text based on the prompt
    response = co.generate(
        model='command-xlarge',  # Choose the model you want to use
        prompt=prompt,  # The prompt text
        stop_sequences=["\n"]  # Optionally set stop sequences
    )

    # Print and return the generated text
    print(response.generations[0].text)
    return response.generations[0].text
