[project]
name = "bp-percentile"
version = "0.5.0"
description = "MCP server for BP percentile calculation"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
authors = [
    {name = "Hyung-Chul Lee", email = "vital@snu.ac.kr"}
]
dependencies = [
    "numpy>=1.20.0",
    "scipy>=1.7.0",
    "mcp-server>=0.5.0",
]

[project.scripts]
bp-percentile = "bp_percentile.__main__:main"
