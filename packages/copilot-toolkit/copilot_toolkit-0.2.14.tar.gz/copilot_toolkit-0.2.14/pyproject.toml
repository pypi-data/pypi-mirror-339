[project]
name = "copilot-toolkit"
version = "0.2.14"
description = "🚀 A toolkit for coding agents 🚀 Get the most out of GitHub Copilot, Cursor and Co. Powered by flock."
readme = "README.md"
authors = [
    { name = "Andre Ratzenberger", email = "andre.ratzenberger@whiteduck.de" }
]
requires-python = ">=3.10"
dependencies = [
    "flock-core>=0.3.38",
    "questionary>=2.1.0",
    "radon>=6.0.1",
    "rich>=13.9.4",
    "tomli>=2.0.1",
]

[project.scripts]
copilot-toolkit = "copilot_toolkit:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
