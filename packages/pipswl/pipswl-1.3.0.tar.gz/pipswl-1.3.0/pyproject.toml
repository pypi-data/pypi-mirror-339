[project]
name = "pipswl"
version = "1.3.0"
description = "Steam Workshop Download List"
authors = [{ name="lesUchiha" }]
readme = "README.md"
requires-python = ">=3.7"
dependencies = ["requests", "tqdm"]

[project.urls]
"GitHub" = "https://github.com/lesUchiha/pip-swl"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
