[project]
name = "tempo-pypack"
version = "0.1.1"
description = "Python library for tracking runtime and monitoring loop progress."
authors = [
    {name = "Henrique Reis ",email = "henrique.hrf@outlook.com"}
]
license = "MIT"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "tempo_pypack", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
