[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "cli-lm"
version = "0.1.0"
description = "A CLI tool for prompting an LLM"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [
    {name = "", email = "your.email@example.com"}
]
dependencies = [
    "openai",
]

[project.scripts]
clm = "clm.main:main"

[tool.setuptools]
package-dir = {"" = "src"}