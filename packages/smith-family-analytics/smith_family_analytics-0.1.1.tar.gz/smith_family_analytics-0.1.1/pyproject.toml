[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "smith-family-analytics"
version = "0.1.1"
description = ""
authors = ["Hayden Smith <hayden.smith@gmail.com>"]
packages = [{include = "analytics"}]

[tool.poetry.dependencies]
python = "^3.12"
pandas = "^2.2.3"
flake8 = "^7.2.0"

[tool.poetry.group.test.dependencies]
pytest = "^8.3.5"
coverage = "^7.8.0"
pytest-cov = "^6.1.0"

[tool.pytest.ini_options]
pythonpath = [
  "."
]