[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "spider_factory"
version = "0.0.8"
dependencies = ["rusta_crawler"]
authors = [
  { name="David Momeni", email="dav.momeni@gmail.com" },
]
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
