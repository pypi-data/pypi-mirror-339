# Central configuration for GitHub workflows
# This file contains shared configuration values used across multiple workflows

# Python configuration
python:
  version: "3.12" # Default Python version to use in workflows

# Package configuration
package:
  name: "openfga-mcp"

# Repository configuration
repository:
  default_branch: "main"
