# -*- coding: utf-8 -*-

import importlib
import importlib.util
import os
import setuptools

"""This file is used to package the SerialNumberTags plugin.

- It was generated by the InvenTree Plugin Creator tool - version 1.3.0
- Ref: https://github.com/inventree/plugin_creator
"""

"""Read the plugin version from the source code."""
module_path = os.path.join(os.path.dirname(__file__), "serial_number_tags", "__init__.py")
spec = importlib.util.spec_from_file_location("serial_number_tags", module_path)
serial_number_tags = importlib.util.module_from_spec(spec)
spec.loader.exec_module(serial_number_tags)

with open('README.md', encoding='utf-8') as f:
    long_description = f.read()

setuptools.setup(
    name="inventree-serial-number-tags",
    version=serial_number_tags.PLUGIN_VERSION,
    author="Oliver Walters",
    author_email="oliver.henry.walters@gmail.com",
    description="Enforce unique serial numbers based on part tags",
    long_description=long_description,
    long_description_content_type='text/markdown',
    url="https://github.com/SchrodingersGat/inventree-serial-tags",
    license="MIT",
    packages=setuptools.find_packages(),
    include_package_data=True,
    install_requires=[
        # Enter your plugin library dependencies here
    ],
    setup_requires=[
        "wheel",
        "twine",
    ],
    python_requires=">=3.9",
    entry_points={
        "inventree_plugins": [
            "SerialNumberTags = serial_number_tags.core:SerialNumberTags"
        ]
    },
)
