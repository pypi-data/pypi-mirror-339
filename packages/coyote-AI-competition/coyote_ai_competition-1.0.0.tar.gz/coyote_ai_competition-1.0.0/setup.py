from setuptools import setup, find_packages

setup(
    name='coyote_game',
    version='0.1.0',
    description='コヨーテボードゲームの進行管理ライブラリ',
    author='Your Name',
    packages=find_packages(),
    install_requires=[
        # 依存ライブラリがあれば(おそらくない？)
    ],
)
