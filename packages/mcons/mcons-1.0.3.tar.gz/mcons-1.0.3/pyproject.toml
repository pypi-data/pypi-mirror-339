
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mcons"
version = "1.0.3"
description = "a project building tool"
dependencies = [
  "PyYAML",
  "watchact"
]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License"
]