[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/logger"]

[project]
name = "r00logger"
version = "0.1.2"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
    "python-dotenv"
]

