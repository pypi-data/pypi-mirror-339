[project]
name = "scanpy-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shuang", email = "shenghui.huang@unito.it" }
]
requires-python = ">=3.10"
dependencies = [
    "scanpy",
    "mcp",
    "pydantic"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
scmcp = "scanpy_mcp.cli:run"
