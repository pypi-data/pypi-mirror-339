[project]
name = "cl-assistant"
version = "1.0.1"
description = "Command Line AI Assistant"
authors = [{ name = "Paulo Lira" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = [
    "httpx"
]

[project.scripts]
cla = "cla.__main__:cli"

[build-system]
requires = ["setuptools", "wheel", "build"]
build-backend = "setuptools.build_meta"
