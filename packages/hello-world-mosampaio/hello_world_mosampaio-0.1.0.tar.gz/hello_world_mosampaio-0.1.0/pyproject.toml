[project]
name = "hello-world-mosampaio"  # Must be globally unique!
version = "0.1.0"
description = "A simple Hello World package"
authors = [{ name="Marcos", email="mosampaio@gmail.com" }]
readme = "README.md"
requires-python = ">=3.7"
license = { text = "MIT" }

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"