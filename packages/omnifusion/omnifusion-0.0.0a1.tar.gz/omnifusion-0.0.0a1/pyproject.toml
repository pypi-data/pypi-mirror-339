[tool.poetry]
name        = "omnifusion"
version     = "0.0.0a1"                       # bump so tag v0.0.0a1 matches
description = "Placeholder package to reserve the omnifusion namespace."
authors     = [
  "Prodigal AI <info@prodigal.ai>",          # ← valid “Name <email>” syntax
  "Nishchal Gaba <nishgaba.ai@gmail.com>"
]
license     = "BSD-3-Clause"
readme      = "README.md"

# IMPORTANT: make sure the file src/omnifusion/__init__.py exists
packages    = [{ include = "omnifusion", from = "src" }]

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry-core>=1.7.0"]
build-backend = "poetry.core.masonry.api"
