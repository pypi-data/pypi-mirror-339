[project]
name = "klipper-jsonrpc"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rui Kobayashi", email = "roboroborx782@gmail.com" }
]
requires-python = ">=3.10"
license = { file = "LICENSE" }
dependencies = [
    "aiohttp>=3.11.16",
]

[project.scripts]
klipper-jsonrpc = "klipper_jsonrpc:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
