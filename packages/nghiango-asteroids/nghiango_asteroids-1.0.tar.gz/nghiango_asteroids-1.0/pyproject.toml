[project]
name = "nghiango-asteroids"
version = "1.0"
dependencies = [
    "pygame",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
asteroids = "asteroids:main"
